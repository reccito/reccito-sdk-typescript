name: Build

# Build-only: verifies the committed generated client still compiles.
# This SDK is machine-generated from Reccito's public OpenAPI spec --
# do not hand-edit generated files.

on:
  push:
    branches: [main]
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - run: npm install
      - run: npm run build
